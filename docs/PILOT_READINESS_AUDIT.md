# ANYQ — сверка хартии пилота и контракта готовности с кодом

**Дата сверки:** 2026-09-08
**Обновлено:** 2026-09-08 — три названных блокера закрыты (раздел 6), P0 контракта
готовности закрыт полностью (раздел 3), P1 и точечные дыры закрыты, кроме
перечисленного в разделе 8
**Ветка:** `phase0-trust-layer`
**Метод:** проверка по коду и схеме, не по памяти. Каждый вывод ниже опирается
на файл, поле или тест.

Этот документ сверяет [`PILOT_CHARTER.md`](PILOT_CHARTER.md) и
[`PRODUCT_READINESS.md`](PRODUCT_READINESS.md) с тем, что реально построено.

## Как читать

Два документа ставят **разные планки**, и держать их раздельно важно.

- **Хартия** — планка одного магазина на тридцать дней.
- **Контракт готовности** — планка «до масштабирования на десятки клиентов»,
  как в нём и написано.

Смешивать их означает либо зря отложить пилот, либо продать неготовое.

**Вывод:** по хартии пилот закрыт почти полностью; по контракту готовности —
нет, и на этом этапе не должен быть.

---

## 1. Хартия: что пилот обязан доказать (раздел 2)

| Пункт | Состояние | Чем подтверждается |
|---|---|---|
| Кассир сам открывает смену, продаёт, возвращает, закрывает | готово | POS: смена, продажа, `/pos/returns`, закрытие с пересчётом кассы |
| Каждое движение объясняется документом и журналом | готово | `StockMovement` с автором, причиной, документом и ячейкой; сверка журнала с остатком |
| При потере связи чек не теряется и не дублируется | готово | `apps/api/src/integration/invariants.test.ts` — повтор с тем же ключом даёт один документ |
| Владелец видит расхождения по деньгам и top-SKU | готово | сводка владельца: касса по сменам, выбросы, расхождения, залежи |
| Измеримый эффект за 30 дней | требует пилота | — |

Все операции из границ пилота (раздел 1) существуют: продажа, возврат, приёмка,
ячейки, перемещение, инвентаризация, B2B-резерв.

## 2. Расхождения хартии с кодом

### 2.1. Офлайн умеет только касса — закрыто

Хартия включает в пилот ячейки, приёмку и пересчёт и требует «обязательно
проверить отключение сети» (раздел 1, строка «Интернет»).

**Было:** офлайн-очередь только у продаж. При обрыве связи кладовщик
останавливался.

**Стало:** очередь команд склада (`apps/pos/src/outbox.ts`). Приёмка,
размещение, списание и пересчёт по ячейкам всегда пишутся на устройство и
уходят сами. Ведомости пересчёта кэшируются, иначе офлайн нечего считать.

Правило при отказе сервера здесь **обратное кассе**: очередь останавливается, а
не пропускает команду. Каждая складская команда рассчитана на мир, который
создала предыдущая.

Устаревший пересчёт применяется как разница, которую он утверждал, а не как
абсолютная цифра: сервер отматывает журнал к `countedAt`. Иначе пересчёт,
пролежавший в очереди до вечера, отменил бы дневную торговлю.

Проверено: `apps/api/src/integration/offline-warehouse.test.ts` — 11 сценариев
против живой базы.

### 2.2. Хартия обещает XLSX — закрыто

Раздел 5: «30–100 SKU… XLSX/CSV».

**Стало:** `.xlsx` читается напрямую (`apps/api/src/xlsx.ts`). Парсер написан
руками, а не взят зависимостью, по той же причине, что и TOTP: у готового пакета
своя история уязвимостей, а нужно из него мало — распаковать две записи и прочесть
их XML. Стили, темы и всё, где мог бы спрятаться макрос, даже не распаковываются.

Вставка из буфера и CSV работают как раньше.

### 2.3. Восстановление резервной копии — закрыто

Раздел 7, «Go в коммерческий платёжный запуск»: «проверено восстановление
резервной копии».

**Стало:** `scripts/backup.mjs` и процедура в
[`BACKUP_RUNBOOK.md`](BACKUP_RUNBOOK.md). `npm run backup:verify` снимает копию,
разворачивает её во временную базу и **доказывает** три вещи: строк столько же,
схема на той же миграции, остаток сходится с журналом движений.

Третья проверка — единственная, которая про этот продукт. Копия, восстановившая
все строки и сломавшая инвариант, прошла бы любую общую проверку и всё равно
была бы бесполезной.

Сама проверка проверена: из копии дампа вырезали десять строк движений, и
`verify` отчитался и о недостаче строк, и — независимо — о разошедшемся
остатке, с кодом возврата 1.

**Остаётся ручным:** хранение копий вне машины. На пилоте это процедура, а не
код.

### 2.4. KPI, которые нечем измерить — закрыто

Раздел 6 требует «успешная синхронизация операций ≥ 99.9%». Контракт готовности
добавляет «p95 < 500 мс».

**Стало:** `apps/api/src/metrics.ts` считает по каждому маршруту долю отказов и
перцентили; `GET /metrics` за тем же общим секретом, что и обслуживающие хуки,
отдаёт оба числа из хартии и называет маршруты, которые их не держат.

Среднее тут не помогло бы: маршрут, отвечающий 40 мс девяноста девяти покупателям
и 9 секунд сотому, в среднем даёт 130 мс и выглядит здоровым, а магазин помнит
очередь.

Отказ клиента (4xx) считается отдельно от сбоя (5xx). «Нет товара» и «этот номер
операции уже использован» — это сервер, делающий свою работу; смешать их с
падениями значит сделать из занятого и корректного магазина аварийный и научить
всех не смотреть на цифру.

---

## 3. Контракт готовности: P0 — «нельзя продавать без этого»

| | Пункт | Состояние |
|---|---|---|
| 1 | Единая идемпотентная очередь **всех** изменяющих команд | **готово для операций с товаром и деньгами.** Ключ поддерживают продажи, возвраты, импорт, приёмка, списание, размещение, пересчёт по ячейкам и по точке, платежи, перемещения, карантин, производство. Заказы поставщику и подтверждение заказа защищены условным переходом статуса внутри транзакции — гарантия сильнее ключа. Проверено: `repeat-safety.test.ts`, каждый маршрут дважды подряд и дважды одновременно |
| 2 | Атомарные резервы и списания с конкурентным доступом; тесты на гонки | **готово.** Условное уменьшение остатка на заблокированной строке; четыре одновременные попытки на одну единицу продают её один раз |
| 3 | ККМ/ОФД адаптер с очередью и сверкой | **частично, и остаток заблокирован снаружи.** Очередь, экран несверенных чеков и ручной провайдер работают. Воркер, разбирающий очередь, написан и покрыт 17 тестами: отступ по времени, различение обрыва связи от отказа ОФД, сдача после исчерпания попыток. Сетевой адаптер — скелет, и в нём прямо написано, чего не хватает: реальной формы запроса провайдера, для которой нужна их действующая документация и тестовая касса. Угадать её нельзя |
| 4 | Полный audit trail, экспорт и восстановление резервной копии | **готово.** Движения товара несут автора, причину, документ и ячейку. Изменения цен, ролей, PIN-кодов, кода НКТ и кредитных лимитов логируются с автором и временем; цена, опущенная и возвращённая одной рукой за сутки, выносится отдельно. Бэкап с проверяемым восстановлением (2.3). Выгрузка в CSV: товары, остатки, продажи построчно, журнал движений, контрагенты — с BOM и точкой с запятой, чтобы открывалось в Excel с русской локалью |
| 5 | Тенант-изоляция, MFA владельца, отзыв устройств, rate limits, мониторинг | **готово.** Изоляция арендаторов — была дыра в `/pos/sales` и `/pos/shifts`, закрыта и покрыта тестом. Отзыв токенов через `tokenVersion`. Лимит частоты на всех мутациях. MFA на платформенной учётной записи: TOTP по RFC 6238 (проверено контрольными векторами стандарта), коды восстановления хешированы и одноразовы, выключение требует обоих факторов. Мониторинг — см. 2.4 |

## 4. Контракт готовности: P1

**Готово:** массовый импорт с отчётом о качестве данных; номенклатура упаковок и
GTIN; резервы под заказы; закупки у поставщика с согласованием и частичной
приёмкой; кредитный контроль; партии, FEFO, просрочка, карантин; приборная
панель исключений вместо графиков.

Сборка и частичная отгрузка заказа сделаны: собранное и заказанное хранятся
отдельно (`pickedQuantity`), ноль — это находка, а не отсутствие, отгрузка снимает
резерв с того, чего не нашли. Возврат поставщику сделан: возврат по конкретной поставке, с учётом уже
возвращённого, кредит считается по цене упаковки — так же, как её читает долг
поставщику, — и уменьшает долг как кредит-нота, без движения денег. Офлайн-склад
сделан (2.1).

## 5. Точечные дыры в «обязательных сценариях»

| Дыра | Где заявлена | Состояние |
|---|---|---|
| Смешанная оплата | Касса: «наличные/карта/смешанная» | **готово.** Продажа несёт строки оплаты, по одной на способ; в денежный ящик идёт только наличная часть |
| Выгрузка данных | Владелец | **готово.** Пять наборов в CSV |
| НКТ в фискальном чеке | Казахстан | **готово.** Задаётся в карточке товара, попадает в фискальную строку, логируется при изменении |
| Русский и казахский | Казахстан | **готово.** Весь интерфейс кассы — касса, склад, экраны владельца, сообщения об офлайне. Проверяется обходом по `apps/pos/src`: вне словаря кириллицы не осталось. Не переводятся данные самого магазина (названия товаров, единицы) и текст ошибок сервера — это сказано в профиле |
| Блокировка ячейки | Склад | **готово.** Ячейка держится целиком; суммы идут в ту же колонку `blocked`, а документ помнит, сколько взял именно этот блок — иначе снятие освободило бы партию, которую кто-то изолировал по другой причине |
| Фото накладной | Приёмка | **готово.** Снимок сжимается на телефоне (~150 КБ), сервер его не декодирует — только проверяет магические байты и размер. Тип берётся из содержимого, не из заявленного, плюс `nosniff` |
| Сборка заказа | Заказы | **готово.** Сборка по стеллажам, частичная отгрузка, стадия выводится из количеств |
| Переход от KPI к документам | Владелец | **готово.** Один эндпоинт с фильтрами (смена, сотрудник, товар, тип) и экран; строки смен и выбивающихся кассиров стали кнопками |
| Часовой пояс локации | Казахстан | **проверено, не блокирует.** На сервере ничего не группируется по календарным дням — все окна скользящие от «сейчас», а клиент показывает в поясе устройства. Казахстан с марта 2024 — единая зона UTC+5. Понадобится, когда появятся отчёты по календарным дням или сеть в разных поясах |

---

## 6. Приоритеты, которые я бы поменял — закрыты

Три вещи стояли между продуктом и первым магазином, и ни одна не была новой
функцией. Все три сделаны 2026-09-08:

1. **Бэкап и проверенное восстановление** — `scripts/backup.mjs`,
   [`BACKUP_RUNBOOK.md`](BACKUP_RUNBOOK.md). См. 2.3.
2. **Офлайн для склада** — очередь команд склада. См. 2.1.
3. **Идемпотентность на остальных мутациях** — см. 3, пункт 1.

По ходу третьего нашлась настоящая гонка, а не просто отсутствующий ключ:
подтверждение зарезервированного заказа читало статус вне транзакции и писало
его безусловно, так что два менеджера на двух устройствах — или один двойной
клик — оба проходили проверку и оба снимали товар с полки. Отклонение имело ту
же форму и снимало резерв дважды, чего не поймала бы никакая сверка журнала:
товар не двигается, полка просто начинает предлагать то, что уже обещано.
Исправлено условным захватом статуса внутри транзакции.

Фискальный сетевой адаптер, мост для печати и выгрузка в 1С
**не блокируют пилот**: при отдельной зарегистрированной ККМ рядом с кассой
правовой контур закрыт, а ручной провайдер записывает, что чек фискализирован.

## 8. Что осталось, и почему

**Сетевой адаптер ОФД.** Заблокирован снаружи: нужна действующая документация
провайдера и тестовая касса. Воркер, очередь, отступы и различение ошибок готовы
и протестированы; адаптеру остаётся один HTTP-вызов. На пилоте правовой контур
закрыт отдельной зарегистрированной ККМ рядом с кассой и ручным провайдером.

**Текст ошибок сервера — по-русски.** Интерфейс кассы переведён целиком:
касса, склад, экраны владельца и те сообщения, которые касса пишет сама, когда
сервера не видно. Обход по `apps/pos/src` не находит кириллицы вне словаря.
Остался следующий слой — 380 русских строк в `apps/api` (158 из них — текст
ошибок, остальное — названия типов документов, причин движения и колонок в
выгрузке), которые касса показывает как есть. На пилот это не влияет: текст
ошибок появляется только при отказе, и частые из них касса уже перекрывает
своим текстом. Перенести их в тот же словарь
нельзя — язык выбирается на устройстве, а не на сервере, так что сервер должен
отвечать кодом причины, а не готовой фразой. Это отдельная работа, не перевод.

**Часовой пояс локации.** Проверено: не блокирует. На сервере ничего не
группируется по календарным дням, все окна скользящие от «сейчас», клиент
показывает в поясе устройства, Казахстан с марта 2024 — единая зона UTC+5.
Понадобится, когда появятся отчёты по календарным дням или сеть в разных поясах.

**Хранение копий вне машины.** На пилоте процедура, а не код — см.
[`BACKUP_RUNBOOK.md`](BACKUP_RUNBOOK.md).

---

## 7. Чего в этой сверке нет

Она сверяет **код с документами**. Она не проверяет: реальное оборудование
(сканеры, принтеры, терминалы), производительность под нагрузкой, соответствие
фискального чека действующим реквизитам и юридическую приёмку персональных
данных. Всё это остаётся за отдельной приёмкой, как и написано в разделе
«Definition of Done» контракта готовности.
