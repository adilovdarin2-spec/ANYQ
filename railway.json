{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run generate --workspace=packages/db && npm run build:deploy"
  },
  "deploy": {
    "startCommand": "node scripts/railway-start.js"
  }
}
