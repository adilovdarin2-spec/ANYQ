{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run generate --workspace=packages/db && npm run build --workspace=apps/admin && npm run build --workspace=apps/pos && npm run build --workspace=apps/orders"
  },
  "deploy": {
    "startCommand": "node scripts/railway-start.js"
  }
}
