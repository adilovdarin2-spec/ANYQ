-- Составляющая спецификации бывает дробной.
--
-- Та же ошибка, что и с количеством в партии, и в тот же день: остаток
-- хранится дробным, расход считается от него, а количество в спецификации было
-- целым. Четверть килограмма ореха на пачку записалась бы нулём.
--
-- До 15.09.2026 это было недостижимо по другой причине: маршрута, который
-- создаёт спецификацию, в продукте не существовало вовсе, и таблица
-- наполнялась только руками. Маршрут появляется — значит и тип должен быть
-- правильным раньше него, а не после первого испорченного рецепта.
ALTER TABLE "recipe_ingredients" ALTER COLUMN "quantity" TYPE DOUBLE PRECISION;
