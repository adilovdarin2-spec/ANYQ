-- Основание выбытия, а не только факт.
--
-- Списание и возврат поставщику убирают упаковку из магазина, и по коду было
-- видно лишь «его больше нет». На сверке спрашивают не состояние, а документ:
-- когда, по какой причине и кто подписал.
ALTER TABLE "marked_codes" ADD COLUMN "retiredDocumentId" TEXT;
