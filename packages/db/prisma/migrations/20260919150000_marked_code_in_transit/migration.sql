-- Код едет вместе с товаром.
--
-- До этого перемещение между точками двигало остаток и партии, а коды
-- оставались на отправителе: привезённую пачку получатель продать не мог
-- («этот код в другой точке»), а отправитель не мог — товара нет. Пачка
-- становилась непродаваемой с обеих сторон.
ALTER TABLE "marked_codes" ADD COLUMN "transferDocumentId" TEXT;
