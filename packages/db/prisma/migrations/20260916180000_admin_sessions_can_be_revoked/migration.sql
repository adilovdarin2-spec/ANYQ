-- Версия токена у платформенного аккаунта.
--
-- До этого его сессию нельзя было отобрать ничем: токен жил семь дней, и
-- включение второго фактора его не касалось — то есть замок вешался на дверь,
-- за которой человек уже сидит. За этой дверью все компании сразу.
ALTER TABLE "admin_users" ADD COLUMN "tokenVersion" INTEGER NOT NULL DEFAULT 0;
