-- Товар продаётся только по коду маркировки.
--
-- Без этого признака защита была добровольной: кассир подносил сканер к
-- штрихкоду вместо Data Matrix, чек уходил без кодов, и сервер его принимал.
ALTER TABLE "products" ADD COLUMN "marked" BOOLEAN NOT NULL DEFAULT false;
