# Points at the Railway-managed Postgres instance (shared by local dev and prod for now).
DATABASE_URL="postgresql://user:password@host:5432/railway"

# Used only by prisma/seed.ts to set the initial admin password — falls back to a dev default if unset.
ADMIN_SEED_PASSWORD="change-me"
